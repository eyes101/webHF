{
  "name": "halfcon-root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "build": "cd frontend && npm install && npm run build && cd ../functions && npm install"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1"
  }
}
